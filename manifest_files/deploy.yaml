apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-01
  labels:
    app: deployment-01
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deployment-01
  template:
    metadata:
      labels:
        app: deployment-01
    spec:
      containers:
      - name: deployment-01
        image: elidrydocker/node-k8s:latest
        ports:
        - containerPort: 3000