apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-01
  namespace: dev
  labels:
    app: deployment-01
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deployment-01
  template:
    metadata:
      labels:
        app: deployment-01
    spec:
      containers:
      - name: deployment-01
        image: wil42/playground:v1
        ports:
        - containerPort: 80