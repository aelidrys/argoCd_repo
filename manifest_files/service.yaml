apiVersion: v1
kind: Service
metadata:
  name: service-01
  namespace: gitlab
spec:
  selector:
    app: deployment-01
  type: NodePort
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 3000
      nodePort: 30085
